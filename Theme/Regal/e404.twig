{% extends 'layout.twig' %}
{% block title trans('errors.404.title') %}

{% block body %}
<div class="container mt-5">
    <div class="page-error">
        <div class="page-inner">
            <h1 style="font-size: 300px;">404</h1>
            <div class="page-description"  style="font-size: 35px;">
                {{ trans("errors.404.paragraph", {'%uri%': uri}) }}
            </div>
            <div class="mt-3">
                <a href="{{ path('account') }}"
                   class="btn btn-primary btn-lg">{{ trans("admin.backtosite") }}</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}